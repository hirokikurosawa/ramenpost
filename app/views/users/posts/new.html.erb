<h1 class="title">新規投稿を作成</h1>
<%= form_with model: @post, local:true do |f| %>
  <h4>画像</h4>
  <%= f.attachment_field :post_image %>
  <h4>コメント</h4>
  <%= f.text_area :introduction %>
  <%= f.submit '投稿' %>
<% end %>