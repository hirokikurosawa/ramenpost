<%= form_with model:@user,url: admins_user_path, local: true do |f| %>
  <div class="form">
    <h2 class="form__title">プロフィール編集</h2>
    <div>
      <p class="form__name"><%= f.label :フルネーム %></p>
      <%= f.text_field :name, autofocus: true, class: "form__field" %>
    </div>

    <div>
      <p class="form__name"><%= f.label :ユーザーネーム（１５文字以内） %></p>
      <%= f.text_field :nickname, autofocus: true, class: "form__field" %>
    </div>

    <div>
      <p class="form__name"><%= f.label :メールアドレス %></p>
      <%= f.text_field :email, autofocus: true, class: "form__field" %>
    </div>

    <div>
      <p class="form__name"><%= f.label :プロフィール写真 %></p>
      <%= attachment_image_tag @user, :profile_image, format: 'jpg', fallback: "default.png", class: "form__icon" %>
      <%= f.attachment_field :profile_image, class: "form__field" %>
    </div>
    <%= link_to "アカウント削除する", admins_user_path(@user), method: :delete,  "data-confirm" => "本当に削除しますか？" %>

    <div class="form__submit">
      <%= f.submit "保存する" %>
    </div>
  </div>
<% end %>

