<div class="follow-page">
  <h1 class="follow-page__title">フォロー</h1>
  <% if @users.empty? %>
   <div class="follow-page__text">フォローしているユーザーはいません。</div>
  <% else%>
    <%= render 'users/users/followings_followers', users: @users %>
  <% end %>
</div>
